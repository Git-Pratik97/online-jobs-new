import { render, screen } from '@testing-library/react';
// import App from './App';

describe('true is truthly 1', () => {
    test('true is thruthly 1', () => {
    expect(true).toBe(true);
  });
  test('false is false 1', () => {
    expect(false).toBe(false);
  });
  });